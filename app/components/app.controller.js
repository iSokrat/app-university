import app from '../app.module.js';

const controller = [
'appQuery',
function(appQuery) {
	// appQuery.universityInfo.jsonp({
	// 	name: 'middle'
	// }).$promise
	// .then((data) => {
	// 	console.log(data);
	// }).catch(console.error);

	this.data = [
		[1, 2, 3],
		[1, 2, 3],
		[1, 2, 3],
		[1, 2, 3],
		[1, 2, 3]
	];

	this.header = [
		'Name',
		'Country',
		'Web Page',
	];

	this.
}];

app.controller('appController', controller);

